{
  "installCommand": "cd ../.. && corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && corepack enable && pnpm --filter frontend build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "git": {
    "ignoreCommand": "cd ../.. && corepack enable && pnpm dlx turbo-ignore"
  }
}
